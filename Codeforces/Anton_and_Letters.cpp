#include<bits/stdc++.h   //443/A
using namespace std;
int main()
{
    string letters;
    cin>>letters;
    // string letters_only="";
    int length=letters.length();
    // letters.erase(0,0);
    // letters.erase(length-1,length-1);
    // cout<<letters<<endl;
    cout<<length<<endl;
    // length-=2;
    // for (int i = 0; i < length; i+=3)
    // {
    //     letters_only+=letters[i];
    // }
    // cout<<letters_only<<endl;
    return 0;
}