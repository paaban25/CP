#include <bits/stdc++.h>
using namespace std;
int main()
{
    int q;
    cin>>q;
    int arr[q];
    int currentIndex=-1;
    for (int i = 0; i < q; i++)
    {
        arr[i]=0;
    }
    while (q--)
    {
        string s;
        cin>>s;
        if (s[0]=='+')
        {
            /* code */
        }
        else if (s[0]=='-')
        {
            /* code */
        }
        else if (s[0]=='!')
        {
            /* code */
        }
        
    }
    
    
    return 0;
}